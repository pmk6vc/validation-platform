---
phase: 01-native-capture-cutover
plan: 05
type: execute
wave: 2
depends_on: ["02", "03", "04"]
files_modified:
  - tap/internal/platformclient/platformclient.go
  - tap/internal/platformclient/platformclient_test.go
  - tap/internal/collectorclient/collectorclient.go
  - tap/internal/collectorclient/collectorclient_test.go
  - tap/internal/configclient/configclient.go
  - tap/internal/configclient/configclient_test.go
  - tap/internal/transformer/transformer.go
  - tap/internal/transformer/transformer_test.go
  - tap/internal/wire/wire.go
  - tap/cmd/vp-tap/main.go
autonomous: true
requirements: [CAPTURE-03, CAPTURE-04, CAPTURE-06]
user_setup: []
must_haves:
  truths:
    - "PlatformClient POSTs /api/services with bearer JWT; classifies responses as Success (201/409) / PermanentRejection (400/422) / TransientFailure (everything else)"
    - "ConfigClient GETs /api/agent/config with bearer JWT and decodes AgentConfigResponse including redactionSalt and the SEC-09 hook fields"
    - "CollectorClient POSTs /api/captured-inputs with gzip-compressed JSON body, bearer JWT, exponential-backoff retry (200ms → 1.6s → 6.4s, max 3) on transient errors; permanent errors fail fast"
    - "Wire DTO CreateCapturedInputRequest matches the existing Kotlin shape byte-for-byte, plus an additive optional `truncated` field"
    - "Transformer filters ReassembledPair by service target + samples + calls redaction.Engine before producing wire DTOs"
  artifacts:
    - path: tap/internal/wire/wire.go
      provides: "DTOs mirroring collector/platform wire contracts"
      contains: "type BatchCreateCapturedInputRequest"
    - path: tap/internal/platformclient/platformclient.go
      provides: "RegisterService + RegistrationOutcome classification"
      contains: "type RegistrationOutcome"
    - path: tap/internal/collectorclient/collectorclient.go
      provides: "Gzip POST with retry"
      contains: "gzip.NewWriter"
    - path: tap/internal/configclient/configclient.go
      provides: "GET /api/agent/config decoder"
      contains: "AgentConfigResponse"
    - path: tap/internal/transformer/transformer.go
      provides: "ReassembledPair → CreateCapturedInputRequest with filter + sample + redaction"
      contains: "func Transform"
  key_links:
    - from: tap/internal/transformer/transformer.go
      to: tap/internal/redaction/redaction.go
      via: "calls redaction.Engine.RedactHeaders + RedactBody before wire emission"
      pattern: "redaction.Engine"
    - from: tap/internal/collectorclient/collectorclient.go
      to: tap/internal/wire/wire.go
      via: "marshals BatchCreateCapturedInputRequest"
      pattern: "BatchCreateCapturedInputRequest"
---

<objective>
Implement the three HTTP client packages (platform, collector, config), the transformer (ReassembledPair → wire DTO + redaction + filter + sample), and the wire DTOs that mirror the existing Kotlin contracts byte-for-byte plus an additive `truncated` field.

Purpose: CAPTURE-03 (config polling, K8s registration with RegistrationOutcome semantics, JWT bearer); CAPTURE-04 (gzip POST + retry + permanent-fail). Closes the loop from BPF capture → wire emission.

Output: four new internal packages + transformer + wire DTOs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-native-capture-cutover/01-CONTEXT.md
@.planning/phases/01-native-capture-cutover/01-RESEARCH.md
@collector/src/main/kotlin/com/platform/collector/models
@platform/src/main/kotlin/com/platform/api/Requests.kt
@agent/src/main/kotlin/com/platform/agent/PlatformClient.kt
@agent/src/main/kotlin/com/platform/agent/CollectorClient.kt
@agent/src/main/kotlin/com/platform/agent/ConfigClient.kt
@agent/src/main/kotlin/com/platform/agent/TrafficTransformer.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define wire DTOs in tap/internal/wire</name>
  <files>tap/internal/wire/wire.go</files>
  <read_first>collector/src/main/kotlin/com/platform/collector/models/CreateCapturedInputRequest.kt (or equivalent file), platform/src/main/kotlin/com/platform/api/Requests.kt, agent/src/main/kotlin/com/platform/agent/models/CapturedInputRequest.kt</read_first>
  <action>Create package `wire`. Declare Go structs mirroring the Kotlin DTOs with `json:` tags matching the existing JSON shape exactly: `CreateServiceRequest{Namespace string `json:"namespace"`; Name string `json:"name"`; Provider string `json:"provider,omitempty"`; Metadata map[string]string `json:"metadata,omitempty"`}`. `AgentConfigResponse{TargetServices map[string]string `json:"targetServices"`; SamplingRate float64 `json:"samplingRate"`; BatchSize int `json:"batchSize"`; CaptureIntervalMs int64 `json:"captureInterval"`; ConfigPollIntervalMs int64 `json:"configPollInterval"`; DiscoveryIntervalMs int64 `json:"discoveryInterval"`; NamespaceFilters []string `json:"namespaceFilters"`; RedactionSalt string `json:"redactionSalt,omitempty"`; ExtraRedactedHeaders []string `json:"extraRedactedHeaders,omitempty"`; ExtraBodyRedactionPatterns []string `json:"extraBodyRedactionPatterns,omitempty"`}`. `CreateCapturedInputRequest{ServiceID string `json:"serviceId"`; InputType string `json:"inputType"`; Method string `json:"method"`; URL string `json:"url"`; RequestHeaders map[string]string `json:"requestHeaders,omitempty"`; RequestBody string `json:"requestBody,omitempty"`; ResponseStatus int `json:"responseStatus"`; ResponseHeaders map[string]string `json:"responseHeaders,omitempty"`; ResponseBody string `json:"responseBody,omitempty"`; LatencyMs int64 `json:"latencyMs,omitempty"`; SourceIP string `json:"sourceIp,omitempty"`; DestinationIP string `json:"destinationIp,omitempty"`; CapturedAt string `json:"capturedAt"`; Truncated bool `json:"truncated,omitempty"`}`. `BatchCreateCapturedInputRequest{Items []CreateCapturedInputRequest `json:"items"`}` + `BatchCreateCapturedInputResponse{Created int `json:"created"`}`. CapturedAt uses RFC3339Nano time format. RequestHeaders/ResponseHeaders use `map[string]string` (not `[]string` — Kotlin model uses `Map<String, String>?`); when a header has multiple values, join with ", " per HTTP convention. **CRITICAL: verify the Kotlin DTOs by reading the actual files first** — field names and presence/absence of `?` (nullable) translate to `omitempty` on the Go side.</action>
  <verify>cd tap &amp;&amp; go vet ./internal/wire/...</verify>
  <acceptance_criteria>
    - tap/internal/wire/wire.go defines all five DTOs (CreateServiceRequest, AgentConfigResponse, CreateCapturedInputRequest, BatchCreateCapturedInputRequest, BatchCreateCapturedInputResponse)
    - All field names + json tags match the Kotlin equivalents byte-for-byte
    - The `truncated` field is added with `json:"truncated,omitempty"` (additive evolution)
    - The redactionSalt, extraRedactedHeaders, extraBodyRedactionPatterns fields are present on AgentConfigResponse
    - `go vet ./internal/wire/...` clean
  </acceptance_criteria>
  <done>Wire DTOs ready; collector/platform clients import them.</done>
</task>

<task type="auto">
  <name>Task 2: PlatformClient with RegistrationOutcome semantics</name>
  <files>tap/internal/platformclient/platformclient.go, tap/internal/platformclient/platformclient_test.go</files>
  <read_first>tap/internal/wire/wire.go, agent/src/main/kotlin/com/platform/agent/PlatformClient.kt</read_first>
  <action>Create package `platformclient`. Sealed-style return: `type RegistrationOutcome interface{ isOutcome() }` with three implementing types: `OutcomeSuccess{ServiceID string}`, `OutcomePermanentRejection{Status int; Detail string}`, `OutcomeTransientFailure{Err error}` (each has an empty isOutcome() method). `Client{baseURL, apiKey string; httpClient *http.Client}` with `New(baseURL, apiKey string) *Client` setting a 10s timeout and a User-Agent. `RegisterService(ctx context.Context, req wire.CreateServiceRequest) RegistrationOutcome`: marshals req, POSTs to baseURL+"/api/services" with Authorization: Bearer apiKey. 201 or 409 → unmarshal response body for serviceId (POST returns the created or existing Service object) → OutcomeSuccess{ServiceID: resp.id}. 400 or 422 → OutcomePermanentRejection{Status, Detail: body string}. Everything else (incl. context errors, network errors, 5xx, 401, 403, 429) → OutcomeTransientFailure{Err: ...}. Write platformclient_test.go using httptest.NewServer: case 201/200 success; case 409 success; case 400/422 permanent rejection; case 500 transient; case network error transient; case 401 transient (not permanent — critical per CONTEXT.md decision narrowing). Test that bearer header is attached on every request.</action>
  <verify>cd tap &amp;&amp; go test ./internal/platformclient/...</verify>
  <acceptance_criteria>
    - platformclient.go exports Client, New, RegisterService, and the three RegistrationOutcome variants
    - 400/422 classify as OutcomePermanentRejection; 401/403/429/5xx classify as OutcomeTransientFailure (CONTEXT.md decision narrowing)
    - platformclient_test.go has at least 6 cases covering all outcome paths
    - All tests pass
  </acceptance_criteria>
  <done>Platform registration client ready.</done>
</task>

<task type="auto">
  <name>Task 3: ConfigClient for GET /api/agent/config</name>
  <files>tap/internal/configclient/configclient.go, tap/internal/configclient/configclient_test.go</files>
  <read_first>tap/internal/wire/wire.go, agent/src/main/kotlin/com/platform/agent/ConfigClient.kt</read_first>
  <action>Create package `configclient`. `Client{baseURL, apiKey string; httpClient *http.Client}`. `New(...) *Client`. `Fetch(ctx context.Context) (*wire.AgentConfigResponse, error)`: GET baseURL+"/api/agent/config" with bearer header. 200 → json.Unmarshal into AgentConfigResponse, return it. Non-200 → return error wrapping status + body. Use `Decoder.DisallowUnknownFields = false` so additive fields don't break the agent. Test: happy path with sample JSON containing all fields including redactionSalt; missing fields (older platform) → defaults apply (zero values); 500 → error.</action>
  <verify>cd tap &amp;&amp; go test ./internal/configclient/...</verify>
  <acceptance_criteria>
    - configclient.go exports Client, New, Fetch
    - Fetch tolerates unknown fields (additive evolution)
    - At least 3 test cases (happy path, partial response, 500 error)
    - All tests pass
  </acceptance_criteria>
  <done>Config polling client ready.</done>
</task>

<task type="auto">
  <name>Task 4: CollectorClient with gzip POST + exponential backoff retry</name>
  <files>tap/internal/collectorclient/collectorclient.go, tap/internal/collectorclient/collectorclient_test.go</files>
  <read_first>tap/internal/wire/wire.go, agent/src/main/kotlin/com/platform/agent/CollectorClient.kt</read_first>
  <action>Create package `collectorclient`. `Client{baseURL, apiKey string; httpClient *http.Client}`. `New(...) *Client` with 30s timeout. `SendBatch(ctx context.Context, batch wire.BatchCreateCapturedInputRequest) error`: marshal to JSON, gzip-compress (compress/gzip default level), POST to baseURL+"/api/captured-inputs" with Authorization: Bearer + Content-Type: application/json + Content-Encoding: gzip. Retry policy: max 3 attempts with backoff 200ms → 1600ms → 6400ms; retry on 5xx + network errors + 429; do NOT retry on 4xx (other than 429) — those are permanent. Record metrics: vp_tap_collector_batches_total{status="ok"|"retry"|"permanent_fail"}, vp_tap_collector_batch_post_duration_seconds, vp_tap_collector_batch_errors_total{status_code=...}. Write collectorclient_test.go: happy 200; first attempt 503 → retry → 200; persistent 503 → max retries → returns error + permanent_fail counter; 400 → no retry + permanent_fail counter; 429 → retry (one of the conditions to flag).</action>
  <verify>cd tap &amp;&amp; go test ./internal/collectorclient/...</verify>
  <acceptance_criteria>
    - collectorclient.go uses compress/gzip, sets Content-Encoding: gzip, Authorization: Bearer
    - Retry policy 200ms → 1.6s → 6.4s exactly; max 3 attempts
    - Metrics increment with documented labels
    - At least 5 test cases (happy, retry-then-success, max-retry-fail, 4xx no-retry, 429 retry)
    - All tests pass
  </acceptance_criteria>
  <done>Collector client ready; transformer pipes ReassembledPair through it.</done>
</task>

<task type="auto">
  <name>Task 5: Transformer ties ReassembledPair → redaction → wire DTO + filter + sample</name>
  <files>tap/internal/transformer/transformer.go, tap/internal/transformer/transformer_test.go</files>
  <read_first>tap/internal/reassembly/reassembly.go, tap/internal/redaction/redaction.go, tap/internal/wire/wire.go, agent/src/main/kotlin/com/platform/agent/TrafficTransformer.kt</read_first>
  <action>Create package `transformer`. `Transformer{eng *redaction.Engine; rand *rand.Rand}`. `New(eng *redaction.Engine) *Transformer`. `Transform(pair *reassembly.ReassembledPair, podLabel, podNamespace string, targetServices map[string]string, samplingRate float64) (*wire.CreateCapturedInputRequest, bool)`: (1) look up serviceID := targetServices[podLabel]; if empty, return (nil, false) and increment a dropped-not-targeted counter. (2) Sample: if rand.Float64() &gt;= samplingRate, return (nil, false). (3) Build CreateCapturedInputRequest: ServiceID=serviceID, InputType="HTTP", Method=pair.Request.Method, URL=pair.Request.URL, ResponseStatus=pair.Response.Status, LatencyMs=pair.LatencyNs/1_000_000, CapturedAt=time.Unix(0, pair.Request.TsNs?).Format(RFC3339Nano) — or fall back to time.Now(). Headers: copy map[string][]string into map[string]string joining values with ", ". (4) Call eng.RedactHeaders on RequestHeaders + ResponseHeaders maps; eng.RedactBody on body bytes with content-type from headers. (5) Set Truncated = pair.Request.Truncated || pair.Response.Truncated. Return (req, true). Write transformer_test.go: case service-mapped + sampled-in → returns non-nil with redacted Authorization; case service-not-mapped → returns nil; case sample-rate=0 → returns nil; case content-type=application/json + token in body → token redacted.</action>
  <verify>cd tap &amp;&amp; go test ./internal/transformer/...</verify>
  <acceptance_criteria>
    - transformer.go exports Transformer, New, Transform
    - Transform calls redaction.Engine.RedactHeaders + RedactBody
    - At least 4 test cases including a positive redaction assertion
    - All tests pass
  </acceptance_criteria>
  <done>Transformer ties pipeline together.</done>
</task>

<task type="auto">
  <name>Task 6: Wire transformer + collector client into main.go's pairsCh consumer</name>
  <files>tap/cmd/vp-tap/main.go</files>
  <read_first>tap/cmd/vp-tap/main.go, tap/internal/transformer/transformer.go, tap/internal/collectorclient/collectorclient.go</read_first>
  <action>In main.go: instantiate redaction engine after first config poll (use the salt from DynamicConfig.RedactionSalt loaded by configclient). Until first config poll completes, buffer ReassembledPair into a bounded channel (capacity = 1024) and drop with a counter when full. After first successful poll, drain buffer and start the transformer + batcher + collector POST goroutine. Batcher: collect up to BatchSize pairs OR up to CaptureIntervalMs, whichever comes first; call collectorClient.SendBatch. Service-target lookup uses DynamicConfig.TargetServices (also kept fresh by the config polling goroutine). For Plan 05 scope, the K8s service-discovery loop is still a placeholder — Plan 06 fills that in; for now main.go can run with TargetServices empty (no captures sent, but no crashes).</action>
  <verify>cd tap &amp;&amp; go build ./cmd/vp-tap &amp;&amp; go vet ./...</verify>
  <acceptance_criteria>
    - main.go instantiates collector client + config client + transformer
    - Batcher honors BatchSize and CaptureIntervalMs
    - First-config-poll bootstrap is bounded by a backpressure channel
    - Build + vet clean
  </acceptance_criteria>
  <done>End-to-end capture → wire pipeline runnable (sans service discovery, which Plan 06 adds).</done>
</task>

</tasks>

<verification>
- [ ] `cd tap && go test ./...` exits 0
- [ ] `cd tap && go build ./cmd/vp-tap` succeeds
- [ ] `cd tap && go vet ./...` clean
- [ ] Manual smoke: running vp-tap against a stubbed collector POSTs a gzip body whose JSON parses into BatchCreateCapturedInputRequest
</verification>

<success_criteria>
- All four client packages + transformer + wire DTOs land
- RegistrationOutcome narrowing (400/422 only as PermanentRejection) preserved
- Retry policy matches RESEARCH §5 + CAPTURE-04 (200ms → 1.6s → 6.4s; max 3)
- Wire shape byte-for-byte compatible with collector (plus additive `truncated`)
</success_criteria>

<output>
After completion, create `.planning/phases/01-native-capture-cutover/01-05-SUMMARY.md`
</output>
