---
phase: 01-native-capture-cutover
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tap/cmd/vp-tap/main.go
  - tap/internal/config/config.go
  - tap/internal/config/config_test.go
  - tap/internal/metrics/metrics.go
  - tap/internal/metrics/metrics_test.go
  - tap/internal/logging/logging.go
  - tap/go.mod
  - tap/go.sum
autonomous: true
requirements: [CAPTURE-04, CAPTURE-06]
user_setup: []
must_haves:
  truths:
    - "Agent loads static config from env vars (PLATFORM_URL, COLLECTOR_URL, API_KEY, KUBESHARK_URL ignored) and behavioral defaults from a ConfigMap-mounted YAML file at /etc/vp-tap/config.yaml"
    - "Agent exposes a Prometheus /metrics endpoint on :9090 alongside /ready"
    - "Three goroutine entry points (capture, service-discovery, config-polling) are wired through context.Context and share a typed DynamicConfig store (atomic.Pointer[DynamicConfig])"
    - "Structured JSON logs are emitted via Go log/slog with INFO/WARN/ERROR levels and parameterized {} placeholders"
  artifacts:
    - path: tap/internal/config/config.go
      provides: "Static + DynamicConfig types, YAML loader, env-var precedence rules"
      contains: "type StaticConfig struct"
    - path: tap/internal/metrics/metrics.go
      provides: "Prometheus registry, metric definitions per RESEARCH §7, promhttp handler factory"
      contains: "vp_tap_ringbuf_drops_total"
    - path: tap/cmd/vp-tap/main.go
      provides: "Goroutine-pipeline orchestration with context.Context lifecycle"
      contains: "atomic.Pointer[config.DynamicConfig]"
  key_links:
    - from: tap/cmd/vp-tap/main.go
      to: tap/internal/config/config.go
      via: "loads StaticConfig once at startup"
      pattern: "config.LoadStatic"
    - from: tap/internal/metrics/metrics.go
      to: tap/cmd/vp-tap/main.go
      via: "metrics endpoint served on :9090"
      pattern: "promhttp.HandlerFor"
---

<objective>
Replace the prototype's flat main.go with a goroutine-pipeline orchestration shell, hybrid config loader (env + ConfigMap YAML), structured logging, and Prometheus /metrics endpoint.

Purpose: D-01 (single Go binary, DaemonSet), D-02 (ConfigMap YAML config), D-03 (goroutine pipeline + context.Context). This plan delivers the skeleton other plans plug into.

Output: `tap/internal/config/`, `tap/internal/metrics/`, `tap/internal/logging/` packages + reorganized `tap/cmd/vp-tap/main.go`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-native-capture-cutover/01-CONTEXT.md
@.planning/phases/01-native-capture-cutover/01-RESEARCH.md
@tap/cmd/vp-tap/main.go
@tap/go.mod
@agent/src/main/kotlin/com/platform/agent/AgentConfig.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create internal/config package with StaticConfig and DynamicConfig types and YAML loader</name>
  <files>tap/internal/config/config.go, tap/internal/config/config_test.go</files>
  <read_first>tap/cmd/vp-tap/main.go, agent/src/main/kotlin/com/platform/agent/AgentConfig.kt, .planning/phases/01-native-capture-cutover/01-RESEARCH.md</read_first>
  <action>Create package `config` in `tap/internal/config/`. Define `StaticConfig` struct with fields PlatformURL, CollectorURL (defaults to PlatformURL when empty), APIKey, NodeName (loaded from env: PLATFORM_URL, COLLECTOR_URL, API_KEY, NODE_NAME). Define `DynamicConfig` struct with fields TargetServices map[string]string, SamplingRate float64, BatchSize int, CaptureIntervalMs int64, ConfigPollIntervalMs int64, DiscoveryIntervalMs int64, NamespaceFilters []string, ExtraRedactedHeaders []string, ExtraBodyRedactionPatterns []string, RedactionSalt string. Define `BehavioralDefaults` struct with fields MaxBodyBytes (default 1048576), MaxHeaderBytes (default 65536), ReassemblyIdleTTL time.Duration (default 30s), AttributionQuarantineSeconds int (default 5), MaxConcurrentConnections int (default 10000) — loaded from YAML at /etc/vp-tap/config.yaml, configurable. Implement `LoadStatic() (*StaticConfig, error)` reading env vars and validating required fields. Implement `LoadBehavioral(path string) (*BehavioralDefaults, error)` using gopkg.in/yaml.v3; if path missing, return defaults. Implement `DynamicConfig.DefaultFor()` returning a zeroed-but-sane DynamicConfig. Add gopkg.in/yaml.v3 to go.mod via `go get gopkg.in/yaml.v3@v3.0.1`. Write config_test.go covering: missing PLATFORM_URL → error; COLLECTOR_URL empty falls back to PLATFORM_URL; YAML loader honors defaults when file absent; YAML loader applies overrides correctly.</action>
  <verify>cd tap &amp;&amp; go test ./internal/config/... &amp;&amp; go vet ./internal/config/...</verify>
  <acceptance_criteria>
    - tap/internal/config/config.go defines StaticConfig, DynamicConfig, BehavioralDefaults structs
    - tap/go.mod contains "gopkg.in/yaml.v3 v3.0.1"
    - config_test.go has at least 4 test cases (missing required env, fallback URL, YAML defaults, YAML overrides)
    - `go test ./internal/config/...` exits 0
  </acceptance_criteria>
  <done>Config types + loader compile and tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create internal/metrics package with Prometheus registry and named metrics</name>
  <files>tap/internal/metrics/metrics.go, tap/internal/metrics/metrics_test.go, tap/go.mod, tap/go.sum</files>
  <read_first>tap/cmd/vp-tap/main.go, .planning/phases/01-native-capture-cutover/01-RESEARCH.md</read_first>
  <action>Create package `metrics` in `tap/internal/metrics/`. Add github.com/prometheus/client_golang v1.20.5 to go.mod (`go get`). Declare a private `*prometheus.Registry` and expose `Registry() *prometheus.Registry` + `Handler() http.Handler` (wraps promhttp.HandlerFor). Define and register every metric listed in RESEARCH.md §7 with exactly the names there: RingbufFillRatio (Gauge), RingbufDropsTotal (CounterVec [reason]), HTTPPairsCapturedTotal (CounterVec [service, method]), HTTPPairsDroppedTotal (CounterVec [reason]), CollectorBatchesTotal (CounterVec [status]), CollectorBatchPostDurationSeconds (HistogramVec [status_code], buckets prometheus.DefBuckets), CollectorBatchErrorsTotal (CounterVec [status_code]), ServiceRegistrationOutcomesTotal (CounterVec [outcome]), InformerFreshnessSeconds (GaugeVec [informer]), RedactionReplacementsTotal (CounterVec [type]), RedactionTruncatedBodiesTotal (Counter), AttributionUnknownTotal (CounterVec [reason]), PreflightStatus (GaugeVec [check]). Export each as a package-level variable so other packages import metrics.RingbufDropsTotal etc. Write metrics_test.go asserting: each metric is registered with the expected name; label sets match the table; CounterVec.WithLabelValues returns non-nil for documented labels.</action>
  <verify>cd tap &amp;&amp; go test ./internal/metrics/... &amp;&amp; go vet ./internal/metrics/...</verify>
  <acceptance_criteria>
    - tap/internal/metrics/metrics.go contains all 13 metric names from RESEARCH §7, prefixed `vp_tap_`
    - tap/go.mod contains "github.com/prometheus/client_golang v1.20.5"
    - metrics_test.go covers every metric registered (one assertion per metric: name + label set)
    - `go test ./internal/metrics/...` exits 0
  </acceptance_criteria>
  <done>Metrics registry and named metrics compile and tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Create internal/logging package with slog JSON handler</name>
  <files>tap/internal/logging/logging.go</files>
  <read_first>tap/cmd/vp-tap/main.go</read_first>
  <action>Create package `logging` in `tap/internal/logging/`. Implement `Setup(level slog.Level)` that calls `slog.SetDefault(slog.New(slog.NewJSONHandler(os.Stdout, &amp;slog.HandlerOptions{Level: level, AddSource: false})))`. Implement `MustLevel(name string) slog.Level` that maps "DEBUG"/"INFO"/"WARN"/"ERROR" to slog.LevelDebug/Info/Warn/Error, falling back to slog.LevelInfo on unknown input. No tests needed (stdlib wrapper).</action>
  <verify>cd tap &amp;&amp; go build ./internal/logging</verify>
  <acceptance_criteria>
    - tap/internal/logging/logging.go contains Setup() and MustLevel() functions
    - Build succeeds
  </acceptance_criteria>
  <done>Structured JSON logging available to all packages.</done>
</task>

<task type="auto">
  <name>Task 4: Refactor main.go into goroutine-pipeline orchestration with /metrics + /ready endpoints</name>
  <files>tap/cmd/vp-tap/main.go</files>
  <read_first>tap/cmd/vp-tap/main.go, tap/internal/config/config.go, tap/internal/metrics/metrics.go, tap/internal/logging/logging.go</read_first>
  <action>Rewrite main.go: (1) call logging.Setup(logging.MustLevel(os.Getenv("LOG_LEVEL"))). (2) Load static := config.LoadStatic() — exit 1 on error. (3) Load behavioral := config.LoadBehavioral("/etc/vp-tap/config.yaml") — log WARN if missing, use defaults. (4) Declare `dyncfg := atomic.Pointer[config.DynamicConfig]{}` and Store the result of `config.DynamicConfig.DefaultFor()`. (5) Set up signal-cancellable context. (6) Open BPF objects and three tracepoint links (existing pattern, extended in Plan 01). (7) Build pod index (existing). (8) Start an HTTP server on :9090 with mux: `/metrics` → metrics.Handler(); `/ready` → 200 if podindex.HasSynced() else 503; `/healthz` → always 200. Use ListenAndServe in a goroutine. (9) Launch goroutines for: shutdownHandler, heartbeat, podInformer, captureLoop (existing) + three new placeholders (serviceDiscoveryLoop, configPollLoop, redactionLoop) which Plans 04–06 fill in — for now they just log "TODO" and return on ctx.Done(). (10) wg.Wait() at the end. Keep four existing helpers (setupLogger, mustRaiseMemlock, etc.) but rename setupLogger → setupStdlibLogger (legacy stub for compatibility, calls logging.Setup).</action>
  <verify>cd tap &amp;&amp; go build ./cmd/vp-tap</verify>
  <acceptance_criteria>
    - main.go imports tap/internal/config, tap/internal/metrics, tap/internal/logging
    - main.go contains `atomic.Pointer[config.DynamicConfig]`
    - main.go starts an HTTP server with at least three handlers: /metrics, /ready, /healthz
    - `go build ./cmd/vp-tap` succeeds and `go vet ./...` passes
    - main.go uses `slog.InfoContext` / `slog.WarnContext` (not stdlib log) for new code paths
  </acceptance_criteria>
  <done>main.go compiles; metrics endpoint serves; ready/health probes work.</done>
</task>

</tasks>

<verification>
- [ ] `cd tap && go build ./cmd/vp-tap` succeeds
- [ ] `cd tap && go test ./...` exits 0
- [ ] `cd tap && go vet ./...` clean
- [ ] Running the binary locally serves `curl http://localhost:9090/metrics` with all 13 metric families exposed
</verification>

<success_criteria>
- Hybrid config loader handles env + YAML + safe defaults
- Prometheus metrics registered per RESEARCH §7 contract
- Structured slog JSON logging
- Goroutine-pipeline shell ready for plans 04–06 to plug into
</success_criteria>

<output>
After completion, create `.planning/phases/01-native-capture-cutover/01-02-SUMMARY.md`
</output>
