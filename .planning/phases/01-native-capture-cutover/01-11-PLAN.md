---
phase: 01-native-capture-cutover
plan: 11
type: execute
wave: 4
depends_on: ["10"]
files_modified:
  - scripts/sandbox-pressure-test.sh
  - .planning/phases/01-native-capture-cutover/01-BENCHMARK.md
autonomous: false
requirements: [CAPTURE-08]
user_setup:
  - service: gcp-sandbox-cluster
    why: "30-minute pressure test runs against the live sandbox cluster (post-Plan 10 cutover)"
    env_vars: []
    dashboard_config:
      - task: "Pressure test runs for 30 minutes; ensure no one else is using the sandbox"
        location: "n/a"
        details: "If sharing, coordinate with team; test consumes node CPU/memory headroom."
must_haves:
  truths:
    - "scripts/sandbox-pressure-test.sh drives 30 minutes of traffic via test-services/traffic-generator against the post-swap sandbox"
    - "Script polls vp-tap /metrics every 30s and records: ringbuf_fill_ratio, ringbuf_drops_total, http_pairs_captured_total, http_pairs_dropped_total, collector_batches_total, attribution_unknown_total"
    - "Script polls Kubernetes Metrics API for vp-tap pod CPU + memory every 30s and records peak values"
    - "Script writes results to .planning/phases/01-native-capture-cutover/01-BENCHMARK.md as a markdown table + pass/fail summary"
    - "Pass criteria: zero panics in vp-tap logs; ringbuf_drops_total / http_pairs_captured_total < 1%; peak CPU < 80% of pod limit; peak memory < 80% of pod limit"
    - "On pass, the benchmark doc commits with a 'Pressure Test: PASS' header — this unblocks decommission (D-12)"
  artifacts:
    - path: scripts/sandbox-pressure-test.sh
      provides: "30-minute pressure-test driver + metrics collector"
      contains: "DURATION=${DURATION:-1800}"
    - path: .planning/phases/01-native-capture-cutover/01-BENCHMARK.md
      provides: "Recorded benchmark results"
      contains: "Pressure Test"
---

<objective>
Implement and execute the 30-minute sandbox pressure test (CAPTURE-08); produce a committed benchmark doc that gates the decommission PR series (D-12).

Purpose: CAPTURE-08 + ROADMAP success criterion #4. Establishes the unblock signal between sandbox swap (Plan 10) and decommission (Plan 12).

Output: `scripts/sandbox-pressure-test.sh` + `.planning/phases/01-native-capture-cutover/01-BENCHMARK.md`.

**Note on autonomy:** `autonomous: false` because the script runs against the live sandbox and takes 30 minutes; the executor pauses for a human-verify checkpoint to confirm the run completed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-native-capture-cutover/01-CONTEXT.md
@.planning/phases/01-native-capture-cutover/01-RESEARCH.md
@scripts/sandbox-up.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement scripts/sandbox-pressure-test.sh</name>
  <files>scripts/sandbox-pressure-test.sh</files>
  <read_first>scripts/sandbox-up.sh, .planning/phases/01-native-capture-cutover/01-RESEARCH.md</read_first>
  <action>Create scripts/sandbox-pressure-test.sh. Shebang `#!/usr/bin/env bash` + `set -euo pipefail`. Env vars: DURATION (default 1800 = 30 minutes), TARGET_RPS_PER_SERVICE (default 200), NAMESPACE (default validation), METRIC_POLL_INTERVAL (default 30). Steps: (1) Verify vp-tap DaemonSet is ready: `kubectl -n $NAMESPACE rollout status ds/vp-tap --timeout=60s`. (2) Verify the traffic-generator workload is running in `production` namespace; if not, deploy it via existing kustomize. (3) Configure traffic-generator to target $TARGET_RPS_PER_SERVICE (the workload's existing RPS knob — read the k8s/test-services manifests to find the env var or arg). (4) Start a metric-poll loop that runs every $METRIC_POLL_INTERVAL seconds for $DURATION seconds: kubectl exec into vp-tap pod, curl localhost:9090/metrics, grep for the six tracked metric names (vp_tap_ringbuf_fill_ratio, vp_tap_ringbuf_drops_total, vp_tap_http_pairs_captured_total, vp_tap_http_pairs_dropped_total, vp_tap_collector_batches_total, vp_tap_attribution_unknown_total). Also `kubectl -n $NAMESPACE top pod -l app=vp-tap --no-headers` for CPU + memory. Append each sample to /tmp/vp-tap-pressure-${TIMESTAMP}.csv. (5) After DURATION elapses, compute deltas + peaks; compute pass criteria (drops/captured ratio < 1%, peak CPU < 80% of limit, peak memory < 80% of limit, panic count = 0 via `kubectl logs ds/vp-tap --since=${DURATION}s | grep -c panic`). (6) Generate the markdown benchmark doc — see Task 2 template. (7) Exit 0 on pass, 1 on fail.</action>
  <verify>bash -n scripts/sandbox-pressure-test.sh &amp;&amp; chmod +x scripts/sandbox-pressure-test.sh</verify>
  <acceptance_criteria>
    - scripts/sandbox-pressure-test.sh has shebang + `set -euo pipefail`
    - Script syntax-checks clean
    - Script is executable
    - Six tracked metrics + CPU/memory captured per sample
    - Pass criteria implemented as documented (panics=0, drops/captured<1%, CPU<80%, mem<80%)
  </acceptance_criteria>
  <done>Script ready to run.</done>
</task>

<task type="auto">
  <name>Task 2: Generate 01-BENCHMARK.md skeleton + commit script for benchmark output</name>
  <files>.planning/phases/01-native-capture-cutover/01-BENCHMARK.md</files>
  <read_first>scripts/sandbox-pressure-test.sh</read_first>
  <action>Create the initial 01-BENCHMARK.md with this structure (placeholder values to be filled by the script): frontmatter (phase: 1, status: pending), top-level "Pressure Test: PENDING" header, "Configuration" subsection listing DURATION + TARGET_RPS + NAMESPACE values, "Results" subsection with a markdown table (columns: Timestamp, ringbuf_fill_ratio, ringbuf_drops_total, http_pairs_captured_total, http_pairs_dropped_total, collector_batches_total, attribution_unknown_total, cpu_millicores, memory_mib), "Summary" subsection with peak values + pass-criteria checklist. The pressure-test script should write into this file using a `BENCH_OUT_FILE` variable defaulting to `.planning/phases/01-native-capture-cutover/01-BENCHMARK.md` and replacing the PENDING header with PASS or FAIL.</action>
  <verify>test -s .planning/phases/01-native-capture-cutover/01-BENCHMARK.md</verify>
  <acceptance_criteria>
    - 01-BENCHMARK.md exists with structure: Configuration, Results table, Summary
    - Pass-criteria checklist matches the 4 documented criteria
  </acceptance_criteria>
  <done>Benchmark doc skeleton ready to receive results.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Pressure test script ready (scripts/sandbox-pressure-test.sh) + benchmark doc skeleton (01-BENCHMARK.md). The 30-minute test against the live sandbox has NOT yet been executed.</what-built>
  <how-to-verify>
  1. Confirm sandbox is up: `kubectl -n validation get ds/vp-tap` shows Ready
  2. Run: `./scripts/sandbox-pressure-test.sh 2>&amp;1 | tee /tmp/pressure-run.log`
  3. After ~30 minutes, verify exit code 0 and `.planning/phases/01-native-capture-cutover/01-BENCHMARK.md` updated with PASS header
  4. Commit the updated benchmark doc
  </how-to-verify>
  <resume-signal>Type "approved" once 01-BENCHMARK.md shows PASS, or "blocked" with the failure mode</resume-signal>
</task>

</tasks>

<verification>
- [ ] scripts/sandbox-pressure-test.sh executable + syntax-clean
- [ ] 01-BENCHMARK.md skeleton present
- [ ] Live test run produces PASS benchmark (human-action checkpoint)
</verification>

<success_criteria>
- 30-min sandbox pressure test passes
- Benchmark doc committed with PASS — unblocks Plan 12 (decommission)
</success_criteria>

<output>
After completion, create `.planning/phases/01-native-capture-cutover/01-11-SUMMARY.md`
</output>
