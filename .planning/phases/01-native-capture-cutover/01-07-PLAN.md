---
phase: 01-native-capture-cutover
plan: 07
type: execute
wave: 3
depends_on: ["01", "02", "04", "05", "06"]
files_modified:
  - tap/internal/preflight/preflight.go
  - tap/internal/preflight/preflight_test.go
  - tap/cmd/vp-tap/main.go
autonomous: true
requirements: [CAPTURE-07]
user_setup: []
must_haves:
  truths:
    - "Pre-flight runs BTF availability check at startup; failure refuses to register and sets vp_tap_preflight_status{check=\"btf\"}=0"
    - "Pre-flight runs loopback HTTP self-test capturing a known-good request/response pair; failure refuses to register and sets vp_tap_preflight_status{check=\"loopback\"}=0"
    - "On failure the agent's pod stays running so /metrics + logs remain scrapable; it just never calls PlatformClient.RegisterService"
    - "Each failure logs an ERROR with check_name + error_class + detail in structured JSON"
  artifacts:
    - path: tap/internal/preflight/preflight.go
      provides: "RunBTFCheck + RunLoopbackHTTPSelfTest"
      contains: "func RunBTFCheck"
  key_links:
    - from: tap/internal/preflight/preflight.go
      to: tap/internal/metrics/metrics.go
      via: "updates vp_tap_preflight_status gauge"
      pattern: "PreflightStatus"
---

<objective>
Implement two pre-flight checks (BTF availability + loopback HTTP self-test) and gate Service registration on them.

Purpose: CAPTURE-07 + ROADMAP success criterion #5. The agent must surface failures so Phase 8 onboarding can read them later — Phase 1 ships metric + log only per RESEARCH §8.

Output: `tap/internal/preflight/` package + main.go integration that refuses to start the service-discovery loop on failure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-native-capture-cutover/01-CONTEXT.md
@.planning/phases/01-native-capture-cutover/01-RESEARCH.md
@tap/cmd/vp-tap/main.go
@tap/internal/reassembly/reassembly.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement BTF + loopback pre-flight checks in tap/internal/preflight/</name>
  <files>tap/internal/preflight/preflight.go, tap/internal/preflight/preflight_test.go</files>
  <read_first>tap/cmd/vp-tap/main.go, tap/internal/metrics/metrics.go, .planning/phases/01-native-capture-cutover/01-RESEARCH.md</read_first>
  <action>Create package `preflight` in tap/internal/preflight/. `RunBTFCheck() error`: stat /sys/kernel/btf/vmlinux; if missing or unreadable, return a wrapped error "BTF unavailable: kernel < 5.8 or BTF stripped". Update metrics.PreflightStatus.WithLabelValues("btf").Set(1 or 0). `RunLoopbackHTTPSelfTest(ctx context.Context, reassembler *reassembly.Reassembler, timeout time.Duration) error`: starts an in-process httptest.Server on 127.0.0.1 with one handler that returns "ok"; fires a single GET request from a client in the same process; the BPF probe should observe both write (request) and read (response) syscalls and the reassembler should emit one ReassembledPair within `timeout`. If a pair is observed → set metrics.PreflightStatus["loopback"].Set(1) and return nil. If no pair within timeout → Set(0) and return error "loopback self-test timed out — kernel may not be capturing write/read syscalls correctly". The test uses a synthetic Feed-side hook on the reassembler to detect emission (the real BPF capture path is the same one used at runtime; this validates the kernel + bpf2go bindings are functional). Build tag: `//go:build linux` on the preflight_test.go file (BPF requires Linux). Tests: TestRunBTFCheck_Present, TestRunBTFCheck_Missing (uses a temp dir as /sys/kernel/btf via a mockable file-system interface — keep simple: skip on non-Linux, just call RunBTFCheck() and assert it succeeds on CI). TestRunLoopbackHTTPSelfTest_Happy — only meaningful inside a fully-attached BPF run, so mark as integration-style and skip unless CGOENV=true or similar env flag; document the manual run path.</action>
  <verify>cd tap &amp;&amp; go test ./internal/preflight/...</verify>
  <acceptance_criteria>
    - preflight.go exports RunBTFCheck and RunLoopbackHTTPSelfTest
    - Both update metrics.PreflightStatus with the corresponding check label
    - preflight_test.go has `//go:build linux` and at minimum a BTF-check test that runs on Linux CI
    - All tests pass
  </acceptance_criteria>
  <done>Pre-flight checks ready.</done>
</task>

<task type="auto">
  <name>Task 2: Gate service registration on preflight outcomes in main.go</name>
  <files>tap/cmd/vp-tap/main.go</files>
  <read_first>tap/cmd/vp-tap/main.go, tap/internal/preflight/preflight.go</read_first>
  <action>In main.go after BPF objects load + tracepoint attach (so the loopback test can observe real captures), run preflight.RunBTFCheck() and preflight.RunLoopbackHTTPSelfTest(ctx, reassembler, 10*time.Second). If EITHER returns error: log ERROR with check_name + error_class + detail; do NOT start the serviceDiscoveryLoop goroutine; DO start the heartbeat + /metrics goroutines so the failure is observable. Every 30s log a structured WARN "vp_tap unhealthy: preflight failed" so log scrapers see ongoing failure. If BOTH succeed: log INFO "preflight passed"; proceed to start serviceDiscoveryLoop and the rest of the goroutine fleet.</action>
  <verify>cd tap &amp;&amp; go build ./cmd/vp-tap &amp;&amp; go vet ./...</verify>
  <acceptance_criteria>
    - main.go calls preflight.RunBTFCheck + preflight.RunLoopbackHTTPSelfTest after BPF attach
    - Failure path: serviceDiscoveryLoop is NOT started; /metrics still served; heartbeat WARN every 30s
    - Build + vet clean
  </acceptance_criteria>
  <done>Pre-flight gates registration; failure stays observable.</done>
</task>

</tasks>

<verification>
- [ ] `cd tap && go test ./...` exits 0
- [ ] `cd tap && go build ./cmd/vp-tap` succeeds
- [ ] Manual: run the binary with /sys/kernel/btf/vmlinux hidden — agent should NOT register but should keep serving /metrics with vp_tap_preflight_status{check="btf"}=0
</verification>

<success_criteria>
- Pre-flight surfaces failures via metric + log (no new platform endpoint — deferred to Phase 8)
- Agent refuses registration on failure but stays observable
</success_criteria>

<output>
After completion, create `.planning/phases/01-native-capture-cutover/01-07-SUMMARY.md`
</output>
