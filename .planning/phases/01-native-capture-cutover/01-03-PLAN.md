---
phase: 01-native-capture-cutover
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tap/internal/redaction/redaction.go
  - tap/internal/redaction/redaction_test.go
  - tap/go.mod
  - tap/go.sum
  - shared/src/main/resources/db/migration/V0008__add_org_redaction_salt.sql
  - platform/src/main/kotlin/com/platform/database/Organizations.kt
  - platform/src/main/kotlin/com/platform/database/OrganizationRepository.kt
  - platform/src/main/kotlin/com/platform/models/Organization.kt
  - platform/src/main/kotlin/com/platform/api/Routes.kt
  - platform/src/main/kotlin/com/platform/api/Requests.kt
  - platform/src/test/kotlin/com/platform/api/RoutesTest.kt
autonomous: true
requirements: [CAPTURE-09]
user_setup: []
must_haves:
  truths:
    - "Redaction engine replaces default-deny headers (Authorization, Cookie, Proxy-Authorization, Set-Cookie, X-API-Key) with typed deterministic placeholders before any network call leaves the agent"
    - "Body redaction is content-type-aware: parses JSON tree-walk for application/json; regex on form/text; skips binary"
    - "Same input → same placeholder (deterministic, per-org salted via DynamicConfig.RedactionSalt); truncated SHA-256 to 6 hex chars"
    - "Body patterns cover JWT-shaped tokens, PAN with Luhn check, Stripe sk_/pk_ prefixes"
    - "Organizations table has a redaction_salt column (V0008 migration) and platform GET /api/agent/config returns it inside AgentConfigResponse"
  artifacts:
    - path: tap/internal/redaction/redaction.go
      provides: "RedactHeaders + RedactBody functions with type-tagged placeholders"
      contains: "func RedactHeaders"
    - path: shared/src/main/resources/db/migration/V0008__add_org_redaction_salt.sql
      provides: "Adds redaction_salt column to organizations"
      contains: "ALTER TABLE organizations ADD COLUMN redaction_salt"
    - path: platform/src/main/kotlin/com/platform/api/Routes.kt
      provides: "GET /api/agent/config returns redactionSalt field"
      contains: "redactionSalt"
  key_links:
    - from: tap/internal/redaction/redaction.go
      to: tap/internal/config/config.go
      via: "reads DynamicConfig.RedactionSalt for per-org hashing"
      pattern: "RedactionSalt"
    - from: platform/src/main/kotlin/com/platform/api/Routes.kt
      to: shared/src/main/resources/db/migration/V0008__add_org_redaction_salt.sql
      via: "OrganizationRepository reads redaction_salt; Routes returns it in AgentConfigResponse"
      pattern: "redaction_salt"
---

<objective>
Implement the redaction engine in the Go agent (header default-deny + content-type-aware body redaction + typed deterministic placeholders) AND wire the per-org redaction salt through the platform: V0008 Flyway migration adds `organizations.redaction_salt`; `GET /api/agent/config` returns it inside `AgentConfigResponse`.

Purpose: CAPTURE-09 + D-14..D-17 + RESEARCH §11. The agent will never POST a request/response containing the default-deny headers or body patterns. Salt management resolved per RESEARCH §11c.

Output: `tap/internal/redaction/` package, V0008 SQL migration, updated Organization model + repository + Routes on the platform side.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-native-capture-cutover/01-CONTEXT.md
@.planning/phases/01-native-capture-cutover/01-RESEARCH.md
@platform/src/main/kotlin/com/platform/database/Organizations.kt
@platform/src/main/kotlin/com/platform/models/Organization.kt
@platform/src/main/kotlin/com/platform/api/Routes.kt
@platform/src/main/kotlin/com/platform/api/Requests.kt
@shared/src/main/resources/db/migration
</context>

<tasks>

<task type="auto">
  <name>Task 1: V0008 Flyway migration adds redaction_salt column</name>
  <files>shared/src/main/resources/db/migration/V0008__add_org_redaction_salt.sql</files>
  <read_first>shared/src/main/resources/db/migration/V0007__*.sql, shared/src/main/resources/db/migration/V0001__*.sql, platform/src/main/kotlin/com/platform/database/Organizations.kt</read_first>
  <action>Create V0008__add_org_redaction_salt.sql. Statements: (1) `ALTER TABLE organizations ADD COLUMN redaction_salt TEXT;` (2) `UPDATE organizations SET redaction_salt = encode(gen_random_bytes(32), 'hex') WHERE redaction_salt IS NULL;` (Postgres `pgcrypto` extension is already present per existing migrations — verify by grep before assuming; if not, add `CREATE EXTENSION IF NOT EXISTS pgcrypto;` as the first statement). (3) `ALTER TABLE organizations ALTER COLUMN redaction_salt SET NOT NULL;`. Use `\n` real newlines, no trailing-comment cruft.</action>
  <verify>ls shared/src/main/resources/db/migration/V0008__add_org_redaction_salt.sql</verify>
  <acceptance_criteria>
    - File V0008__add_org_redaction_salt.sql exists with three statements (ALTER ADD, UPDATE, ALTER SET NOT NULL)
    - File uses `gen_random_bytes(32)` from pgcrypto
    - Migration is idempotent against a fresh DB (verified by the DatabaseTestBase Flyway run in subsequent tests)
  </acceptance_criteria>
  <done>Flyway migration ready; downstream task adds Kotlin model + repository.</done>
</task>

<task type="auto">
  <name>Task 2: Update Organization model + Organizations table + OrganizationRepository to read redaction_salt</name>
  <files>platform/src/main/kotlin/com/platform/models/Organization.kt, platform/src/main/kotlin/com/platform/database/Organizations.kt, platform/src/main/kotlin/com/platform/database/OrganizationRepository.kt</files>
  <read_first>platform/src/main/kotlin/com/platform/models/Organization.kt, platform/src/main/kotlin/com/platform/database/Organizations.kt, platform/src/main/kotlin/com/platform/database/OrganizationRepository.kt</read_first>
  <action>Add `val redactionSalt: String` (non-nullable) to the Organization data class. Add `val redactionSalt = text("redaction_salt")` column to the Organizations Exposed Table object. Update the `ResultRow.toOrganization()` extension (or wherever the mapper lives) to copy redactionSalt. Update OrganizationRepository.create() (if it sets a salt at insert time) — the migration backfills existing rows; new rows go via the create path so add a `redactionSalt = generateHexSalt(32)` default at the model construction site (use java.security.SecureRandom + Hex encoding from BouncyCastle which is already in scope). Add a helper `private fun generateHexSalt(byteLen: Int): String` to OrganizationRepository or a new shared utility file.</action>
  <verify>./gradlew :platform:compileKotlin :platform:test --tests OrganizationRepositoryTest</verify>
  <acceptance_criteria>
    - Organization data class contains `val redactionSalt: String`
    - Organizations Table contains `redaction_salt` column declared as `text("redaction_salt")`
    - OrganizationRepository.create() sets a fresh 64-char hex salt for new organizations
    - Existing OrganizationRepository unit tests continue to pass with the new field
  </acceptance_criteria>
  <done>Platform reads + writes redaction_salt column.</done>
</task>

<task type="auto">
  <name>Task 3: Extend AgentConfigResponse + GET /api/agent/config to include redactionSalt + extra redaction allowlist fields</name>
  <files>platform/src/main/kotlin/com/platform/api/Requests.kt, platform/src/main/kotlin/com/platform/api/Routes.kt, platform/src/test/kotlin/com/platform/api/RoutesTest.kt</files>
  <read_first>platform/src/main/kotlin/com/platform/api/Requests.kt, platform/src/main/kotlin/com/platform/api/Routes.kt</read_first>
  <action>Update `AgentConfigResponse` to add three new fields (all with safe defaults so the additive evolution rule holds and pre-Phase-1 agents that ignore them keep working): `val redactionSalt: String = ""`, `val extraRedactedHeaders: List<String> = emptyList()`, `val extraBodyRedactionPatterns: List<String> = emptyList()`. In Routes.kt for `GET /api/agent/config`, look up the org via OrganizationRepository, set `redactionSalt = organization.redactionSalt`. The extra allowlist fields stay empty in Phase 1 (Phase 3 SEC-09 wires them — design-the-hook-only per D-17). Add a route test asserting the response JSON contains a non-empty redactionSalt matching `[0-9a-f]{64}`.</action>
  <verify>./gradlew :platform:test --tests RoutesTest</verify>
  <acceptance_criteria>
    - AgentConfigResponse contains redactionSalt, extraRedactedHeaders, extraBodyRedactionPatterns fields with defaults
    - GET /api/agent/config response JSON contains a 64-char hex redactionSalt for the caller's org
    - RoutesTest contains an assertion for the redactionSalt field shape (`assertTrue(response.redactionSalt.matches(Regex("[0-9a-f]{64}")))`)
    - ./gradlew :platform:test passes
  </acceptance_criteria>
  <done>Platform serves the salt to authenticated agents.</done>
</task>

<task type="auto">
  <name>Task 4: Implement Go redaction package with header allowlist + content-type-aware body redaction + typed placeholders</name>
  <files>tap/internal/redaction/redaction.go, tap/internal/redaction/redaction_test.go</files>
  <read_first>.planning/phases/01-native-capture-cutover/01-RESEARCH.md, tap/internal/config/config.go (from Plan 02 if landed; otherwise design against the documented type)</read_first>
  <action>Create package `redaction` in `tap/internal/redaction/`. Define `RedactionType` constants: TypeAuthorization, TypeCookie, TypeProxyAuthorization, TypeSetCookie, TypeXAPIKey, TypeJWT, TypePAN, TypeSKToken, TypePKToken, TypeCustom (string-typed). Define `Engine` struct holding the per-org salt + extra header/body allowlist. `NewEngine(salt string, extraHeaders []string, extraBodyPatterns []string) (*Engine, error)`: pre-compiles all body regexes (Go regexp.MustCompile for the hardcoded set + extras with error on bad regex). Implement `RedactHeaders(h map[string][]string)`: for each header name in the default-deny list + extras (case-insensitive), replace the entire value with `placeholder(typeFor(name), salt, originalValue)`. Implement `RedactBody(contentType string, body []byte) []byte`: if contentType starts with "application/json" call `redactJSON(body)`; if "application/x-www-form-urlencoded" or starts with "text/" call `redactText(body)`; else return body unchanged + emit metric vp_tap_redaction_skipped_total{reason="unknown_content_type"}. `redactJSON` uses encoding/json.Decoder + a Decode-and-rewrite approach (decode to interface{}, walk, replace leaf strings matching any compiled pattern with placeholder, re-Marshal). `redactText` runs the compiled regex set with ReplaceAllStringFunc. `placeholder(t, salt, val)`: returns `fmt.Sprintf("<REDACTED:%s:%s>", t, hex.EncodeToString(sha256.Sum256([]byte(salt+":"+val))[:3]))`. Body patterns: JWT = `[A-Za-z0-9_-]{4,}\.[A-Za-z0-9_-]{4,}\.[A-Za-z0-9_-]{4,}`; PAN = `\b(?:\d[ -]*?){13,19}\b` (followed by Luhn check in a custom ReplaceAllStringFunc); sk_test/sk_live = `sk_(?:test|live)_[A-Za-z0-9]{24,}`; pk_test/pk_live = `pk_(?:test|live)_[A-Za-z0-9]{24,}`. Write redaction_test.go with table-driven tests: (a) Authorization header → `<REDACTED:authorization:...>`; (b) two identical Authorization values across calls → identical placeholder (determinism); (c) different salts → different placeholders (per-org isolation); (d) JSON body `{"token":"eyJ...."}` → JSON valid + token replaced; (e) PAN with valid Luhn → replaced; PAN with invalid Luhn → NOT replaced; (f) binary content-type (application/octet-stream) → body passthrough.</action>
  <verify>cd tap &amp;&amp; go test ./internal/redaction/...</verify>
  <acceptance_criteria>
    - tap/internal/redaction/redaction.go exports Engine, NewEngine, RedactHeaders, RedactBody, and RedactionType constants
    - redaction_test.go has at least 6 test cases (header default-deny, determinism, salt isolation, JSON tree-walk, PAN Luhn pos/neg, binary passthrough)
    - `cd tap && go test ./internal/redaction/...` exits 0
    - All hardcoded body regexes from RESEARCH §11e are present and compile
  </acceptance_criteria>
  <done>Redaction engine ready; downstream pipeline plan (04 transformer) calls Engine.</done>
</task>

</tasks>

<verification>
- [ ] `./gradlew :platform:test` passes
- [ ] `./gradlew :shared:test` passes (DatabaseTestBase reruns the new V0008 migration cleanly)
- [ ] `cd tap && go test ./internal/redaction/...` passes
- [ ] `cd tap && go vet ./...` clean
- [ ] GET /api/agent/config returns a 64-char hex redactionSalt + empty allowlist arrays
</verification>

<success_criteria>
- Redaction default-deny header list complete; deterministic typed placeholders
- Body redaction content-type-aware; JSON tree-walk preserves structure
- Per-org salt persisted in Postgres via V0008 + propagated to agent via /api/agent/config
- DynamicConfig hook fields (extraRedactedHeaders, extraBodyRedactionPatterns) exist with empty defaults — Phase 3 SEC-09 populates
</success_criteria>

<output>
After completion, create `.planning/phases/01-native-capture-cutover/01-03-SUMMARY.md`
</output>
