---
phase: 01-native-capture-cutover
plan: 12
type: execute
wave: 4
depends_on: ["11"]
files_modified:
  - k8s/agent/base
  - k8s/agent/overlays
  - deploy/Dockerfile.agent
  - agent
  - settings.gradle.kts
  - collector/src/main/kotlin/com/platform/collector/models
  - e2e-tests/src/test/kotlin/com/platform/e2e/AgentDiscoveryE2ETest.kt
  - CLAUDE.md
autonomous: true
requirements: [CAPTURE-11]
user_setup: []
must_haves:
  truths:
    - "Kotlin agent/ Gradle module fully removed (directory deleted, entry removed from settings.gradle.kts)"
    - "k8s/agent/ overlay tree fully removed"
    - "deploy/Dockerfile.agent removed"
    - "KubesharkClient.kt, Kubeshark wire DTOs, and Kubeshark test fixtures deleted from the repo (no remaining references)"
    - "Kotlin-agent AgentDiscoveryE2ETest deleted; new Go-agent AgentDiscoveryGoE2ETest (from Plan 09) is the only e2e covering agent paths"
    - "CLAUDE.md updated to describe the Go capture path (vp-tap), the Helm chart install, and the removal of the Kotlin agent + Kubeshark"
    - "Each sub-PR is independently revertible; tests stay green between PRs (e2e + unit) — verified by running CI on each sub-PR"
  artifacts:
    - path: CLAUDE.md
      provides: "Updated CLAUDE.md describing the Go capture path"
      contains: "vp-tap"
---

<objective>
Outside-in decommission of the Kotlin agent + Kubeshark in four sequential sub-PRs (per D-10), with integration/e2e tests updated inline so CI is never red between commits.

Purpose: CAPTURE-11. The phase isn't complete until the old path is gone.

Output: Four atomic git commits (one per task below), each landing as its own PR. Plan-checker should treat this plan as four atomic sub-tasks even though they share a PLAN.md — each task ends in a commit + a verification sweep.

**Note on autonomy:** This plan is `autonomous: true` because each sub-task is a deterministic deletion + test re-run. If any sub-task breaks tests, the executor halts and surfaces the failure — the user reverts manually.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-native-capture-cutover/01-CONTEXT.md
@.planning/phases/01-native-capture-cutover/01-RESEARCH.md
@settings.gradle.kts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 (PR 1): Remove k8s/agent + deploy/Dockerfile.agent + remaining sandbox-up.sh refs</name>
  <files>k8s/agent (delete tree), deploy/Dockerfile.agent (delete)</files>
  <read_first>k8s/agent, deploy/Dockerfile.agent, scripts/sandbox-up.sh</read_first>
  <action>Delete the entire k8s/agent/ directory tree (base/, overlays/, all YAML). Delete deploy/Dockerfile.agent. grep -r "k8s/agent" across the repo to find any remaining references (most should have been removed in Plan 10); for any hit, fix it. Same for `deploy/Dockerfile.agent`. After deletion, run `./gradlew :platform:test :collector:test :shared:test` to confirm no module imports broke (none expected — the Kotlin agent module is still in-tree, just deployment artifacts are gone). Commit: `chore(01): remove Kotlin agent deployment artifacts (decom PR 1/4)`.</action>
  <verify>./gradlew :platform:test :collector:test :shared:test &amp;&amp; test ! -e k8s/agent &amp;&amp; test ! -e deploy/Dockerfile.agent</verify>
  <acceptance_criteria>
    - k8s/agent directory does not exist
    - deploy/Dockerfile.agent does not exist
    - No remaining references to either in the repo (grep clean)
    - Unit tests for platform + collector + shared pass
  </acceptance_criteria>
  <done>PR 1 ready.</done>
</task>

<task type="auto">
  <name>Task 2 (PR 2): Remove agent/ Gradle module + settings.gradle.kts entry</name>
  <files>agent (delete tree), settings.gradle.kts</files>
  <read_first>settings.gradle.kts, agent/build.gradle.kts</read_first>
  <action>Remove `include(":agent")` line from settings.gradle.kts. Delete the entire `agent/` directory tree (Kotlin module). Run `./gradlew :e2e-tests:compileTestKotlin` — at this point e2e-tests may still reference Kotlin-agent types (KubesharkClient, etc.) from the AgentDiscoveryE2ETest test class; that test class is deleted in PR 3 but its references break compilation NOW. To keep CI green BETWEEN PRs, delete the references to Kotlin-agent types from AgentDiscoveryE2ETest in this PR (or stub them out / delete the test class entirely IF the Go-agent equivalent (AgentDiscoveryGoE2ETest from Plan 09) already covers the same surface). Recommended: in this PR, delete the AgentDiscoveryE2ETest.kt class entirely (the Go equivalent covers the surface). Run `./gradlew compileTestKotlin test` end-to-end. Commit: `chore(01): remove Kotlin agent Gradle module + Kubeshark e2e (decom PR 2/4)`.</action>
  <verify>./gradlew test &amp;&amp; test ! -e agent &amp;&amp; ! grep -q ":agent" settings.gradle.kts</verify>
  <acceptance_criteria>
    - agent/ directory does not exist
    - settings.gradle.kts does not include `:agent`
    - All Gradle modules compile + test pass
    - AgentDiscoveryE2ETest.kt deleted (replaced by AgentDiscoveryGoE2ETest from Plan 09)
  </acceptance_criteria>
  <done>PR 2 ready.</done>
</task>

<task type="auto">
  <name>Task 3 (PR 3): Remove KubesharkClient.kt + Kubeshark wire DTOs + Kubeshark fixtures + sweep stragglers</name>
  <files>collector/src/main/kotlin/com/platform/collector/models (Kubeshark-specific files), any remaining KubesharkEntry / KubesharkClient references</files>
  <read_first>collector/src/main/kotlin/com/platform/collector/models, e2e-tests/src/test/kotlin/com/platform/e2e</read_first>
  <action>grep -r "Kubeshark" --include="*.kt" across collector/, e2e-tests/, shared/, platform/. For each hit: if the file is named after Kubeshark or contains Kubeshark-only logic, delete it. If it's a shared file with a Kubeshark reference (e.g., a DTO field), remove the field per the additive-evolution rule (Kotlin collector clients ignore unknown JSON fields). Specifically: (a) delete any file matching `**/Kubeshark*.kt`; (b) delete fixture files like `**/kubeshark-*.json` if they exist; (c) update collector/models if any DTO has a `kubesharkSource` / similar discriminator field — remove it. Run `./gradlew test` to ensure no residual references break compilation. Commit: `chore(01): remove Kubeshark wire DTOs + fixtures (decom PR 3/4)`.</action>
  <verify>./gradlew test &amp;&amp; ! grep -r "Kubeshark" --include="*.kt" .</verify>
  <acceptance_criteria>
    - `grep -r Kubeshark --include="*.kt" .` returns no results
    - All Gradle modules test pass
  </acceptance_criteria>
  <done>PR 3 ready.</done>
</task>

<task type="auto">
  <name>Task 4 (PR 4): Update CLAUDE.md to describe the Go capture path</name>
  <files>CLAUDE.md</files>
  <read_first>CLAUDE.md, .planning/phases/01-native-capture-cutover/01-CONTEXT.md, .planning/phases/01-native-capture-cutover/01-RESEARCH.md</read_first>
  <action>Update CLAUDE.md in the relevant sections: (1) "Validation agent" subsection — replace the Kotlin-agent description with a Go-tap description: "Validation tap — single Go binary deployed as a DaemonSet via the helm/vp-tap chart; owns BPF capture, K8s informers (services + pods with cgroup_id attribution + quarantine), userspace HTTP/1.1 reassembly + FIFO pairing, content-type-aware redaction, and JWT-authenticated POST to collector. Located in tap/ (single Go module)." (2) Module ownership table — remove the `agent/` row, add a `tap/` row (Go) and `helm/vp-tap/` row. (3) Architecture vision diagram — update the production-cluster box to show vp-tap instead of Kubeshark + Kotlin agent. (4) "Key Design Decisions" table — add a row for the cutover: "Capture stack: Go eBPF tap + Go agent in one binary (replaces Kubeshark + Kotlin agent). Rationale: zero-instrumentation eBPF capture without an external dependency. Cutover landed in Phase 1." (5) "Development Setup" — remove `./gradlew :agent:run` references; add `make -C tap docker-build` and `helm install vp-tap helm/vp-tap` for the equivalent agent-side workflow. (6) "Tech Stack" — confirm Go 1.26 + cilium/ebpf are listed (already present) and remove agent-side Kotlin/Fabric8 references (Fabric8 stays for platform if any usage remains; remove only if it was solely agent's). Commit: `docs(01): update CLAUDE.md for Go capture path (decom PR 4/4)`.</action>
  <verify>grep -q "vp-tap" CLAUDE.md &amp;&amp; ! grep -q ":agent:run" CLAUDE.md &amp;&amp; ! grep -q "Kubeshark.*active\|currently using Kubeshark" CLAUDE.md</verify>
  <acceptance_criteria>
    - CLAUDE.md describes vp-tap as the active capture path
    - CLAUDE.md no longer describes Kotlin agent or Kubeshark as current state (historical mentions in a "Cutover history" subsection are OK)
    - CLAUDE.md module table includes tap/ + helm/vp-tap/
  </acceptance_criteria>
  <done>PR 4 lands; Phase 1 decommission complete.</done>
</task>

</tasks>

<verification>
- [ ] `./gradlew test && cd tap && go test ./...` exits 0 after EACH sub-task (i.e. after each PR's commit)
- [ ] No grep matches for "Kubeshark", "agent/" (as module), or "KubesharkClient" remain in the repo
- [ ] CLAUDE.md describes the Go capture path
- [ ] Phase 1 ROADMAP success criterion #2 satisfied (Kubeshark removed, vp-tap installed, agent/ + Kubeshark wiring deleted)
</verification>

<success_criteria>
- Four sub-PRs land cleanly (one commit each)
- Tests stay green between commits (no red CI windows)
- CLAUDE.md current with the post-cutover state
- Phase 1 success criteria #2 + #6 (decommission + bilingual CI) verifiably true
</success_criteria>

<output>
After completion, create `.planning/phases/01-native-capture-cutover/01-12-SUMMARY.md`
</output>
