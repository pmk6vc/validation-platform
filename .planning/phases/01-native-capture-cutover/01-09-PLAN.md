---
phase: 01-native-capture-cutover
plan: 09
type: execute
wave: 3
depends_on: ["01", "02", "03", "04", "05", "06", "07", "08"]
files_modified:
  - e2e-tests/build.gradle.kts
  - e2e-tests/src/test/kotlin/com/platform/e2e/AgentDiscoveryGoE2ETest.kt
  - e2e-tests/src/test/kotlin/com/platform/e2e/support/VpTapContainer.kt
  - tap/build.gradle.kts
autonomous: true
requirements: [CAPTURE-12]
user_setup: []
must_haves:
  truths:
    - "A Gradle task `:tap:dockerBuild` builds the vp-tap:test image via `make -C tap docker-build`"
    - "AgentDiscoveryGoE2ETest starts platform + collector + k3s + vp-tap:test container; the container discovers a test service in k3s and ingests captured traffic into the collector"
    - "Test asserts a captured-input row appears in the collector DB within 60 seconds (ROADMAP success criterion #1)"
    - "Test skips with a clear message on non-Linux hosts (BPF can't load on macOS / Windows TestContainers)"
  artifacts:
    - path: e2e-tests/src/test/kotlin/com/platform/e2e/AgentDiscoveryGoE2ETest.kt
      provides: "End-to-end test exercising K8s discovery → registration → traffic capture → collector ingest"
      contains: "class AgentDiscoveryGoE2ETest"
    - path: e2e-tests/src/test/kotlin/com/platform/e2e/support/VpTapContainer.kt
      provides: "TestContainers wrapper around vp-tap:test image"
      contains: "class VpTapContainer"
    - path: tap/build.gradle.kts
      provides: "Gradle wrapper for tap Docker build (shim to make)"
      contains: "dockerBuild"
---

<objective>
Add a Kotlin e2e test in `e2e-tests/` that launches the Go vp-tap container alongside platform + collector + k3s and validates the full capture → ingest loop.

Purpose: CAPTURE-12 + ROADMAP success criterion #1 + D-11 (Go e2e green before sandbox swap).

Output: New e2e test + container wrapper + Gradle build glue + skip-on-non-Linux logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-native-capture-cutover/01-CONTEXT.md
@.planning/phases/01-native-capture-cutover/01-RESEARCH.md
@e2e-tests/build.gradle.kts
@tap/Dockerfile
@tap/Makefile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gradle shim task :tap:dockerBuild</name>
  <files>tap/build.gradle.kts</files>
  <read_first>tap/Makefile, settings.gradle.kts</read_first>
  <action>Create tap/build.gradle.kts with a single Exec task `dockerBuild` that runs `make -C tap docker-build TAG=test IMAGE=vp-tap`. Verify the file imports / plugin block is minimal (no Kotlin plugin needed — this is a pure shell-shim project). Update settings.gradle.kts to include ":tap" if not already. The task should declare an output (the docker image tag) so Gradle's incremental build can skip when nothing under tap/ has changed. Trade-off: this is a thin wrapper; intentionally avoids inventing a Gradle-Go bridge (per "Don't couple where coupling doesn't exist" feedback).</action>
  <verify>./gradlew :tap:tasks --all 2>&amp;1 | grep -q dockerBuild &amp;&amp; echo "task present"</verify>
  <acceptance_criteria>
    - settings.gradle.kts includes :tap
    - `./gradlew :tap:tasks` lists the dockerBuild task
    - Task delegates to `make -C tap docker-build`
  </acceptance_criteria>
  <done>Gradle task exists for downstream test to depend on.</done>
</task>

<task type="auto">
  <name>Task 2: VpTapContainer TestContainers wrapper</name>
  <files>e2e-tests/src/test/kotlin/com/platform/e2e/support/VpTapContainer.kt</files>
  <read_first>e2e-tests/build.gradle.kts, tap/Dockerfile, k8s/tap/daemonset.yaml</read_first>
  <action>Create VpTapContainer extending GenericContainer&lt;VpTapContainer&gt;. Image: "vp-tap:test" (built by Task 1). Constructor params: platformUrl, collectorUrl, apiKey, kubeconfig (path to k3s kubeconfig from KubernetesWorkloadTestBase). Env: PLATFORM_URL, COLLECTOR_URL, API_KEY, NODE_NAME (use container hostname), KUBECONFIG (mount the kubeconfig file). Bind mounts: kubeconfig → /etc/kubeconfig (read-only); /sys/kernel/btf → /sys/kernel/btf (read-only) — note: TestContainers honors this only on Linux hosts. Privileged: true. Network: shared with platform + collector + k3s containers (use `withNetwork(network)`). HealthCheck via curl http://localhost:9090/ready inside the container. Method `assertReady(timeout: Duration)`: blocks until `/ready` returns 200 or timeout. Method `metric(name: String): Double` reads `/metrics`, parses Prometheus text format, returns the named metric's current value (used for assertions).</action>
  <verify>./gradlew :e2e-tests:compileTestKotlin</verify>
  <acceptance_criteria>
    - VpTapContainer.kt compiles
    - Class binds the kubeconfig + BTF mounts and runs privileged
    - Helper methods `assertReady` and `metric(name)` exposed
  </acceptance_criteria>
  <done>Container wrapper ready.</done>
</task>

<task type="auto">
  <name>Task 3: AgentDiscoveryGoE2ETest — full-stack e2e</name>
  <files>e2e-tests/src/test/kotlin/com/platform/e2e/AgentDiscoveryGoE2ETest.kt</files>
  <read_first>e2e-tests/src/test/kotlin/com/platform/e2e/support/VpTapContainer.kt, shared/src/testFixtures/kotlin/com/platform/shared/testing/KubernetesWorkloadTestBase.kt</read_first>
  <action>Create AgentDiscoveryGoE2ETest extending the existing full-stack base (the one used by AgentDiscoveryE2ETest — likely KubernetesWorkloadTestBase or a sibling). At @BeforeAll: skip if `System.getProperty("os.name").lowercase() != "linux"` with message "vp-tap e2e requires Linux — BPF unavailable on macOS/Windows TestContainers". Spin up platform + collector + k3s + an HTTP test service in k3s + vp-tap container. Inject an org-level JWT via TestJwtKeys. Test method `captureLoopProducesIngestedRow()`: (1) trigger HTTP traffic against the test service from within k3s; (2) poll the collector's `GET /api/captured-inputs` (or DB directly via TestContainers DB connection) every 1s up to 60s; (3) assert at least one row with method=GET, url containing the test service path, response status 200; (4) verify `vpTapContainer.metric("vp_tap_http_pairs_captured_total")` > 0 and `vp_tap_collector_batches_total` > 0. Test method `redactionStripsAuthorizationHeader()`: trigger a request with `Authorization: Bearer dummy`; assert the captured row's requestHeaders["Authorization"] starts with "<REDACTED:authorization:".</action>
  <verify>./gradlew :e2e-tests:test --tests AgentDiscoveryGoE2ETest -i</verify>
  <acceptance_criteria>
    - AgentDiscoveryGoE2ETest.kt skips cleanly on non-Linux hosts
    - On Linux CI: tests pass with the full pipeline exercised
    - At least 2 test cases (capture happy path + redaction assertion)
  </acceptance_criteria>
  <done>Bilingual e2e green before sandbox swap (D-11 condition met).</done>
</task>

<task type="auto">
  <name>Task 4: e2e-tests Gradle wiring to depend on :tap:dockerBuild</name>
  <files>e2e-tests/build.gradle.kts</files>
  <read_first>e2e-tests/build.gradle.kts</read_first>
  <action>Add `tasks.test { dependsOn(":tap:dockerBuild") }` so the e2e tests always build a fresh vp-tap:test image before running. Verify the existing pre-test image builds for platform + collector still work (don't disturb them).</action>
  <verify>./gradlew :e2e-tests:test --dry-run 2>&amp;1 | grep dockerBuild</verify>
  <acceptance_criteria>
    - e2e-tests:test depends on :tap:dockerBuild
    - Existing platform + collector image dependencies preserved
  </acceptance_criteria>
  <done>Bilingual CI gate complete.</done>
</task>

</tasks>

<verification>
- [ ] `./gradlew :e2e-tests:compileTestKotlin` succeeds
- [ ] On Linux CI: `./gradlew :e2e-tests:test --tests AgentDiscoveryGoE2ETest` passes
- [ ] On macOS dev: test skips with clear message
- [ ] `./gradlew :tap:dockerBuild` produces vp-tap:test image
</verification>

<success_criteria>
- Bilingual e2e covers K8s discovery → registration → capture → ingest
- Test gates the sandbox-swap PR (D-11)
- Skip-on-non-Linux logic explicit
</success_criteria>

<output>
After completion, create `.planning/phases/01-native-capture-cutover/01-09-SUMMARY.md`
</output>
