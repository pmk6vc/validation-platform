---
phase: 01-native-capture-cutover
plan: 10
type: execute
wave: 4
depends_on: ["07", "08", "09"]
files_modified:
  - scripts/sandbox-up.sh
  - scripts/sandbox-down.sh
  - k8s/agent/overlays/sandbox/kustomization.yaml
autonomous: false
requirements: [CAPTURE-10]
user_setup:
  - service: gcp-sandbox-cluster
    why: "Sandbox GKE cluster must be available; this plan performs a destructive cutover (removes Kubeshark + Kotlin agent from the live sandbox)"
    env_vars: []
    dashboard_config:
      - task: "Verify sandbox cluster is up before running this plan"
        location: "gcloud container clusters list --region us-central1"
        details: "If down, run scripts/sandbox-up.sh first to bring it up."
must_haves:
  truths:
    - "scripts/sandbox-up.sh no longer references Kubeshark — the `helm upgrade --install kubeshark` block is removed"
    - "scripts/sandbox-up.sh installs vp-tap via `helm upgrade --install vp-tap ./helm/vp-tap --namespace validation --create-namespace --set platform.url=$PLATFORM_URL --set platform.collectorUrl=$COLLECTOR_URL`"
    - "k8s/agent/overlays/sandbox/ no longer applied by sandbox-up.sh (Kotlin agent overlay is no longer deployed); the overlay tree is still in the repo until decommission PR 1 (Plan 12) removes it"
    - "Running scripts/sandbox-up.sh on a clean sandbox installs vp-tap and the AgentDiscoveryGoE2ETest passes against the live deployment"
    - "The PR is single-commit revertible — git revert restores the previous Kubeshark + Kotlin agent path with no orphan state"
  artifacts:
    - path: scripts/sandbox-up.sh
      provides: "Sandbox bring-up that installs vp-tap, NOT Kubeshark"
      contains: "helm upgrade --install vp-tap"
---

<objective>
Hard-swap the sandbox cluster's capture stack: remove Kubeshark + Kotlin agent from sandbox-up.sh; install vp-tap via Helm. Single PR, revertible via `git revert`.

Purpose: CAPTURE-10 + ROADMAP success criterion #2 + D-09 (hard swap, no side-by-side).

Output: Updated `scripts/sandbox-up.sh` (and corresponding `sandbox-down.sh` cleanup), Kotlin agent overlay kustomization is no longer applied (overlay files themselves removed by Plan 12).

**Note on autonomy:** This plan is `autonomous: false` because it MUTATES the live GCP sandbox cluster. The executor pauses for a human-verify checkpoint after the swap before continuing to decommission (Plan 12).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-native-capture-cutover/01-CONTEXT.md
@.planning/phases/01-native-capture-cutover/01-RESEARCH.md
@scripts/sandbox-up.sh
@scripts/sandbox-down.sh
@k8s/agent/overlays/sandbox/kustomization.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit scripts/sandbox-up.sh — replace Kubeshark + Kotlin-agent blocks with vp-tap Helm install</name>
  <files>scripts/sandbox-up.sh</files>
  <read_first>scripts/sandbox-up.sh, helm/vp-tap/values.yaml</read_first>
  <action>Locate the existing `helm upgrade --install kubeshark` block and remove it entirely (the comment block above it that explains Kubeshark's role should also be removed; replace with a comment explaining vp-tap is now the capture path). Locate the kustomize-apply block that applies `k8s/agent/overlays/sandbox/` (Kotlin agent) and remove that too (Kotlin agent is no longer deployed to sandbox). Add a new block AFTER the platform + collector are ready: `helm upgrade --install vp-tap ./helm/vp-tap --namespace validation --create-namespace --set platform.url="$PLATFORM_URL" --set platform.collectorUrl="$COLLECTOR_URL" --set image.tag="${VP_TAP_IMAGE_TAG:-latest}" --wait --timeout 5m`. Keep the existing `--build-local` flag handling — if `--build-local` is passed, build the vp-tap image (`make -C tap docker-build TAG=latest IMAGE=$ARTIFACT_REGISTRY/vp-tap` + `make -C tap push TAG=latest IMAGE=$ARTIFACT_REGISTRY/vp-tap`) before the Helm install. Verify the script still substitutes Cloud Run URLs into the `__PLATFORM_URL__` / `__COLLECTOR_URL__` placeholders — those placeholders may no longer exist after the kustomize block removal, in which case the sed lines are dropped too.</action>
  <verify>bash -n scripts/sandbox-up.sh &amp;&amp; grep -L "kubeshark" scripts/sandbox-up.sh &amp;&amp; grep -q "helm upgrade --install vp-tap" scripts/sandbox-up.sh</verify>
  <acceptance_criteria>
    - sandbox-up.sh does NOT contain the string "kubeshark" or "Kubeshark"
    - sandbox-up.sh contains `helm upgrade --install vp-tap ./helm/vp-tap`
    - sandbox-up.sh passes shellcheck (or at least `bash -n` syntax check)
    - The `--build-local` path builds vp-tap locally before deploying
  </acceptance_criteria>
  <done>Script swap complete; ready for human-verify run.</done>
</task>

<task type="auto">
  <name>Task 2: Update scripts/sandbox-down.sh to also remove vp-tap</name>
  <files>scripts/sandbox-down.sh</files>
  <read_first>scripts/sandbox-down.sh</read_first>
  <action>Find the section that uninstalls Kubeshark (`helm uninstall kubeshark ...`) and replace it with `helm uninstall vp-tap --namespace validation --ignore-not-found`. Remove any references to deleting the Kotlin agent overlay (kubectl delete -k k8s/agent/overlays/sandbox/) since the overlay is no longer applied. Idempotency: every helm/kubectl command must use `--ignore-not-found` or equivalent.</action>
  <verify>bash -n scripts/sandbox-down.sh &amp;&amp; grep -q "helm uninstall vp-tap" scripts/sandbox-down.sh</verify>
  <acceptance_criteria>
    - sandbox-down.sh uninstalls vp-tap (idempotent)
    - sandbox-down.sh does NOT reference kubeshark
    - Script syntax-checks clean
  </acceptance_criteria>
  <done>Down-script consistent with the swap.</done>
</task>

<task type="auto">
  <name>Task 3: Live sandbox swap dry-run (--dry-run-server)</name>
  <files>(no files modified)</files>
  <read_first>scripts/sandbox-up.sh</read_first>
  <action>Run `helm upgrade --install vp-tap ./helm/vp-tap --namespace validation --create-namespace --dry-run --debug --set platform.url=https://example.com --set platform.collectorUrl=https://example.com` and capture stdout. Verify the rendered Kubernetes objects look correct (DaemonSet, ClusterRole, ConfigMap, ServiceAccount, ClusterRoleBinding). Document any issues in the eventual SUMMARY.md.</action>
  <verify>helm upgrade --install vp-tap ./helm/vp-tap --namespace validation --create-namespace --dry-run --set platform.url=https://example.com 2>&amp;1 | tail -10</verify>
  <acceptance_criteria>
    - Helm dry-run succeeds with no errors
    - Output contains DaemonSet, ClusterRole, ConfigMap definitions
  </acceptance_criteria>
  <done>Helm install path validated against a server-side dry-run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Sandbox cutover scripts swapped from Kubeshark + Kotlin agent to vp-tap Helm chart. Live sandbox bring-up NOT YET performed (this plan ships the script changes; the human runs scripts/sandbox-up.sh to apply the swap to the live cluster).</what-built>
  <how-to-verify>
  1. Confirm scripts/sandbox-up.sh diff looks correct (no kubeshark refs, vp-tap Helm install present)
  2. (Optional, destructive) Run `./scripts/sandbox-up.sh --build-local` against the live sandbox; observe Kubeshark uninstall + vp-tap DaemonSet ready
  3. Confirm `kubectl -n validation get ds` shows vp-tap with desired=current=ready
  4. Confirm `kubectl -n validation logs ds/vp-tap | head -20` shows pre-flight passing and registration outcomes
  5. Trigger traffic via the test-services traffic-generator; confirm capture rows appear in collector DB
  </how-to-verify>
  <resume-signal>Type "approved" once the live cutover is confirmed working, or "blocked" with the failure mode</resume-signal>
</task>

</tasks>

<verification>
- [ ] `bash -n scripts/sandbox-up.sh && bash -n scripts/sandbox-down.sh` (syntax check)
- [ ] grep -L kubeshark scripts/sandbox-up.sh (no Kubeshark refs)
- [ ] `helm lint helm/vp-tap/` clean
- [ ] Live human-verify checkpoint approved
</verification>

<success_criteria>
- Sandbox bring-up no longer touches Kubeshark
- vp-tap installed via Helm; live cluster verified
- PR single-commit revertible
</success_criteria>

<output>
After completion, create `.planning/phases/01-native-capture-cutover/01-10-SUMMARY.md`
</output>
