---
phase: 01-native-capture-cutover
plan: 06
type: execute
wave: 2
depends_on: ["02", "05"]
files_modified:
  - tap/internal/k8s/services/discovery.go
  - tap/internal/k8s/services/discovery_test.go
  - tap/internal/k8s/podinformer/podinformer.go
  - tap/internal/k8s/podinformer/podinformer_test.go
  - tap/cmd/vp-tap/main.go
  - k8s/tap/rbac.yaml
autonomous: true
requirements: [CAPTURE-02, CAPTURE-03]
user_setup: []
must_haves:
  truths:
    - "K8s Service-discovery informer enumerates Services cluster-wide, validates pod-selector labels per CAPTURE-02, and produces a stream of services to register"
    - "Service-discovery loop diffs against registered + permanently-failed sets and calls PlatformClient.RegisterService for new services"
    - "RegistrationOutcome variants drive the loop: Success → mark registered + update TargetServices map; PermanentRejection → mark permanently-failed; TransientFailure → retry on next tick"
    - "Pod informer extends with attribution-quarantine window: a cgroup_id from a deleted pod is held back for AttributionQuarantineSeconds before accepting a new owner"
    - "K8s RBAC manifest grants only get/list/watch on pods and services (no other verbs, no other resources)"
  artifacts:
    - path: tap/internal/k8s/services/discovery.go
      provides: "Service informer + diff-and-register loop"
      contains: "type ServiceDiscovery struct"
    - path: tap/internal/k8s/podinformer/podinformer.go
      provides: "Extended pod informer with attribution-quarantine logic"
      contains: "quarantineUntil"
    - path: k8s/tap/rbac.yaml
      provides: "ClusterRole with pods + services get/list/watch only"
      contains: "verbs: [\"get\", \"list\", \"watch\"]"
  key_links:
    - from: tap/internal/k8s/services/discovery.go
      to: tap/internal/platformclient/platformclient.go
      via: "calls platformclient.RegisterService"
      pattern: "RegisterService"
    - from: tap/internal/k8s/podinformer/podinformer.go
      to: tap/internal/metrics/metrics.go
      via: "increments AttributionUnknownTotal with reason=quarantined"
      pattern: "AttributionUnknownTotal"
---

<objective>
Implement K8s Service-discovery (Loop 1 equivalent of Kotlin agent) and extend the existing pod informer with attribution-under-churn quarantine logic per RESEARCH §1.

Purpose: CAPTURE-02 (pod-label enrichment + cgroup_id quarantine) + CAPTURE-03 (Service registration with RegistrationOutcome semantics).

Output: new `tap/internal/k8s/services/` package, extended `tap/internal/k8s/podinformer/`, updated `k8s/tap/rbac.yaml` to add services get/list/watch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-native-capture-cutover/01-CONTEXT.md
@.planning/phases/01-native-capture-cutover/01-RESEARCH.md
@tap/internal/k8s/podinformer/podinformer.go
@tap/internal/k8s/podinformer/cgroup_resolver.go
@k8s/tap/rbac.yaml
@agent/src/main/kotlin/com/platform/agent/K8sServiceDiscovery.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tap/internal/k8s/services/ package for Service discovery</name>
  <files>tap/internal/k8s/services/discovery.go, tap/internal/k8s/services/discovery_test.go</files>
  <read_first>agent/src/main/kotlin/com/platform/agent/K8sServiceDiscovery.kt, tap/internal/k8s/podinformer/podinformer.go, tap/internal/platformclient/platformclient.go</read_first>
  <action>Create package `services` in tap/internal/k8s/services/. Define `ServiceDiscovery{client kubernetes.Interface; informer cache.SharedInformer; lister listersv1.ServiceLister}`. `New(client kubernetes.Interface) *ServiceDiscovery`: builds a SharedInformerFactory (NOT field-selector-scoped — Service is cluster-level and we need cross-namespace visibility), 5-minute resync. `Run(ctx context.Context)`: starts the informer, blocks until ctx done. `List() []*corev1.Service`: returns current snapshot of all services. `Loop(ctx context.Context, platformClient *platformclient.Client, cluster string, registered, permanentlyFailed map[string]bool, mu *sync.RWMutex, targetServicesCh chan<- map[string]string, interval time.Duration)`: ticks every interval; for each service not in registered AND not in permanentlyFailed AND not in skipped-namespaces (kube-system, kube-public, kube-node-lease) AND that has a valid `app` label on its selector, call platformClient.RegisterService(name=service.Name, namespace=service.Namespace, provider="KUBERNETES"). Update registered/permanentlyFailed based on RegistrationOutcome. After each tick, send the current registered name→serviceID map down targetServicesCh (non-blocking; latest-wins). Update metrics.ServiceRegistrationOutcomesTotal with the right outcome label. Filter rule: a Service is eligible if `service.Spec.Selector` is non-empty AND the namespace is not in the skip-list. Write discovery_test.go with a fake clientset (k8s.io/client-go/kubernetes/fake): seed 3 services (one valid, one in kube-system, one with empty selector); call Loop once; assert only the valid one was registered.</action>
  <verify>cd tap &amp;&amp; go test ./internal/k8s/services/...</verify>
  <acceptance_criteria>
    - discovery.go exports ServiceDiscovery, New, Run, List, Loop
    - Service filtering excludes kube-system / kube-public / kube-node-lease and services with empty selectors
    - discovery_test.go uses fake clientset and asserts only valid services registered
    - All tests pass
  </acceptance_criteria>
  <done>Service discovery loop ready.</done>
</task>

<task type="auto">
  <name>Task 2: Extend pod informer with cgroup_id attribution-quarantine</name>
  <files>tap/internal/k8s/podinformer/podinformer.go, tap/internal/k8s/podinformer/podinformer_test.go</files>
  <read_first>tap/internal/k8s/podinformer/podinformer.go, tap/internal/k8s/podinformer/cgroup_resolver.go, tap/internal/k8s/podinformer/podinformer_test.go, .planning/phases/01-native-capture-cutover/01-RESEARCH.md</read_first>
  <action>Extend Index struct with: `quarantineUntil map[uint64]time.Time` (cgroup_id → time it leaves quarantine), `quarantineDuration time.Duration` (default 5s). Add `NewIndexWithQuarantine(client, nodeName, resolver, quarantineDuration)`; keep existing NewIndex as a thin wrapper passing 5s default. On Pod Delete, instead of deleting cgroup_id mappings immediately, set quarantineUntil[cgID] = now + quarantineDuration and KEEP the existing entry in cgroupToPod (still attributing in-flight events for a brief window). On Pod Add: if a new pod resolves to a cgroup_id that is in quarantine AND quarantineUntil[cgID] > now, log a WARN ("cgroup_id reuse detected during quarantine window") and DO NOT update cgroupToPod for that cgID until the quarantine expires (events captured in the meantime emit with attribution_unknown counter). Lookup() returns the existing entry if quarantine not yet expired (correctness during the deletion window); after quarantine expires, the index garbage-collects. Add a `RunQuarantineGC(ctx context.Context)` goroutine that sweeps quarantineUntil every 1s and removes expired entries. Update podinformer_test.go: add a TestQuarantineWindow case (create pod, capture cgroup_id, delete pod, immediately attempt to attribute → returns original pod; advance fake clock 6s, attempt → returns not-found); add a TestQuarantineReuse case (create pod A, delete A, immediately create pod B with same cgroup_id → A still wins during quarantine, B wins after expiry).</action>
  <verify>cd tap &amp;&amp; go test ./internal/k8s/podinformer/...</verify>
  <acceptance_criteria>
    - podinformer.go exposes NewIndexWithQuarantine and a configurable quarantineDuration
    - On Pod Delete the mapping persists for quarantineDuration before deletion
    - On Pod Add to a quarantined cgroup_id, the old mapping wins until expiry
    - At least 2 new test cases (TestQuarantineWindow, TestQuarantineReuse)
    - All tests pass
  </acceptance_criteria>
  <done>Pod informer churn-safe per RESEARCH §1.</done>
</task>

<task type="auto">
  <name>Task 3: Update k8s/tap/rbac.yaml to grant services get/list/watch</name>
  <files>k8s/tap/rbac.yaml</files>
  <read_first>k8s/tap/rbac.yaml</read_first>
  <action>Update the ClusterRole to include a second rule for Services: `- apiGroups: [""], resources: ["services"], verbs: ["get", "list", "watch"]`. Keep the existing pods rule. No other resources, no other verbs. Add a header comment explaining: "Pod watch is field-selector-scoped to spec.nodeName by the informer; Service watch is cluster-wide because services are cluster-level resources used for discovery." Verify the ServiceAccount + ClusterRoleBinding are unchanged.</action>
  <verify>kubectl --dry-run=client apply -f k8s/tap/rbac.yaml || true; grep -E "verbs:.*\[\"get\", \"list\", \"watch\"\]" k8s/tap/rbac.yaml</verify>
  <acceptance_criteria>
    - k8s/tap/rbac.yaml ClusterRole grants exactly two rules: pods (g/l/w) and services (g/l/w); no other resources, no other verbs
    - File parses as valid YAML
  </acceptance_criteria>
  <done>RBAC narrowed exactly to what the agent needs.</done>
</task>

<task type="auto">
  <name>Task 4: Wire ServiceDiscovery into main.go's serviceDiscoveryLoop goroutine</name>
  <files>tap/cmd/vp-tap/main.go</files>
  <read_first>tap/cmd/vp-tap/main.go, tap/internal/k8s/services/discovery.go</read_first>
  <action>In main.go: construct ServiceDiscovery with the existing kubernetes.Interface, start its informer goroutine. Replace the serviceDiscoveryLoop placeholder with a real loop calling discovery.Loop(...). Pass a registered+permanentlyFailed map (with sync.RWMutex), and write the updated targetServices map into dyncfg every tick (read the existing dyncfg.Load(), update TargetServices field, Store back). The transformer + collector loop already read dyncfg.Load().TargetServices, so this propagation closes the loop.</action>
  <verify>cd tap &amp;&amp; go build ./cmd/vp-tap &amp;&amp; go vet ./...</verify>
  <acceptance_criteria>
    - main.go uses services.New() and starts services.Run + services.Loop goroutines
    - main.go updates dyncfg.TargetServices after each registration tick
    - Build + vet clean
  </acceptance_criteria>
  <done>Service discovery feeds the transformer's target-services filter.</done>
</task>

</tasks>

<verification>
- [ ] `cd tap && go test ./...` exits 0
- [ ] `cd tap && go build ./cmd/vp-tap` succeeds
- [ ] `cd tap && go vet ./...` clean
- [ ] k8s/tap/rbac.yaml lists exactly the documented verbs
</verification>

<success_criteria>
- Service discovery loop registers + maintains target-services map
- Pod informer survives churn per RESEARCH §1
- RBAC scope narrowed to documented set
</success_criteria>

<output>
After completion, create `.planning/phases/01-native-capture-cutover/01-06-SUMMARY.md`
</output>
